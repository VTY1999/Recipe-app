<% name = current_user.present? ? current_user.email : 'Guest' %>
  <div class="container">
    <h3 class="mt-3">Welcome, <span class="text-success"> <%= name.capitalize().gsub(/@gmail.com/, '') %>!</span> </h3>
  </div>
